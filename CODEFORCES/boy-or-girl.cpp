#include <bits/stdc++.h>
using namespace std;

signed main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    
    set<string> dt_chars;

    string s;
    cin >> s;

    for(int i=0; i<s.size(); i++) dt_chars.insert(s[1]);



    return 0;
}